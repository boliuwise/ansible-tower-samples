---
- name: Install and configure Nginx on Ubuntu and CentOS
  hosts: all
  become: yes
  tasks:
    - name: Install Nginx on Ubuntu
      shell: java -version
      register: get_jdk
      ignore_errors: yes      


